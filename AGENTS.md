# 项目持久化上下文

## 绿、蓝、黄三区目标架构

- 本项目的公司级安全架构基线是[《数据安全策略图（整改目标）》](docs/architecture/数据安全策略图-整改目标.png)。涉及网络拓扑、安全分区、跨区访问、文件流转、研发访问或审计方案时，必须先对照该图。
- 该基线由用户于 2026-07-14 提供并录入项目；仓库内图片与原始附件的 SHA-256 均为 `d03ccd3d2b0b8a5517800e22fd3050b325ab107f46b01b7fb3f88fc5bd465b38`。
- 该图是“整改目标”架构，不等同于所有控制均已在现网落地。描述现状或完成度时，必须以仓库代码、部署配置、测试结果和用户确认的信息为准。
- 图中未给出 IP、网段、端口、协议、证书、账号、负责人、产品版本和具体部署状态。不得从图标或箭头自行推断这些信息；需要用到时应列为待确认项。
- 图中能确认的拓扑和控制目标以原图为准；[《绿蓝黄数据链路白名单台账》](docs/绿蓝黄数据链路白名单台账.md)是业务链路及放行门禁的补充。两者出现歧义时，不得擅自选择，应向用户核实。

### 分区定位

- 绿区：普通办公和传统 PC 区。包含办公终端、云桌面客户端和绿区 NAS，可访问互联网，但终端、文件和公网访问仍受控。
- 黄区：高密隔离桌面云及高密研发区。禁止外网访问，研发和服务器访问实行最严格的隔离、加密、审批与审计。
- 蓝区：高密隔离桌面云及受控研发区。网络访问受限，端侧行为和研发访问需要审计，并通过 WAF 等边界设施访问获准服务。
- 互联网侧：包含公网应用、邮件、ERP、网页、办公平台和公网 AI，不属于任何受控色区。
- 隔离与访问控制强度按业务数据敏感度收紧。任何跨区或出区链路均应默认拒绝、最小放行、身份可识别、过程可审计。

### 绿区控制基线

- 本地策略包括：设备准入、病毒防控、软件管控、文件防泄漏、漏洞扫描和基础管控。
- 普通办公终端使用 EDR 承担病毒防控和漏洞扫描；AC、防火墙承担公网访问限制、NAT、IPS 等边界控制。
- 只有公司配发且安装 EDR 的设备才允许使用云桌面客户端，这是图中明确写出的准入限制。
- 云桌面客户端和绿区 NAS 受动态权限控制；绿区文件流转需要 DLP 防泄漏策略、软件管控和文件流向审计。
- 受控文件外发经过 ODCP 文件外发平台审批，并配合 DLP 文件标记；不得新增绕过审批的临时文件通道。
- 绿区进入高密环境应使用获准的云桌面链路，不得把“终端可联网”理解为可直接访问黄区或蓝区服务器。

### 黄区控制基线

- 本地策略包括：VDI 系统隔离、禁止外网访问、文件自动加密、软件安装管控、代码加密和服务器禁止直连。
- 高密隔离桌面云先经过 AD 域鉴权，再访问黄区 NAS、GitLab 代码库、Jumpserver 等获准资源。
- 研发和项目服务器应经 Jumpserver 等受控入口限制访问并审计；“服务器禁止直连”是硬约束。
- 图示代码交付链路为 GitLab 代码库、Jenkins 打包、VIRBOX 自动加密或代码混淆、生成加密 JAR 包。修改构建或发布方案时必须保留代码保护目标。
- 黄区系统 URL、CIM、研发服务器和项目部服务器之间只允许图示的受控访问关系，不能仅凭网络可达性扩展横向访问。

### 蓝区控制基线

- 本地策略包括：VDI 系统隔离、限制网络访问和端侧行为审计。
- 高密隔离桌面云经过 AD 域鉴权后，访问蓝区 GitLab 代码库和 Jumpserver；研发服务器访问应通过 Jumpserver 等受控入口。
- 蓝区研发服务器访问上层系统 URL 的链路经过 WAF 七层防护，并纳入七层管控和审计。
- 蓝区访问公网 AI 属于受限出站链路，必须经过 WAF 防火墙，不得为方便调用模型而开放任意公网访问。
- WAF 及相关边界设施的日志由日志转储虚拟机收集。新增规则或链路必须保留可关联的访问与拒绝审计信息。

### 对本仓库工作的约束

- 本仓库实现的是七层 WAF 能力，不能声称替代 EDR、AC、防火墙、DLP、ODCP、AD、Jumpserver、VDI、代码加密或日志平台。
- 安全规则默认采用 fail-closed：未明确登记的来源、目标、方向、方法、路径、字段、文件类型或模型不得放行。
- 设计白名单时必须区分方向；“允许 A 访问 B”不自动意味着允许 B 访问 A。
- 不得为调试、构建、模型调用或运维便利增加黄区直连外网、服务器直连、绕过 WAF、绕过 Jumpserver、绕过文件外发审批的路径。
- 跨区数据必须遵守最小必要、内容白名单、敏感信息防泄漏和全链路审计。黄区原始代码、原始数据、密钥、凭证和未脱敏文档不得因接口放通而直接出区。
- 涉及三区链路的变更，至少核对源分区、目标分区、方向、身份、协议端口、接口或数据白名单、审计、回滚，以及是否符合[白名单台账](docs/绿蓝黄数据链路白名单台账.md)的上线门禁。
